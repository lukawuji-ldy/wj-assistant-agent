server:
  port: 8081

spring:
  application:
    name: assistant-mcp-server
  ai:
    mcp:
      server:
        name: wuji-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: "Wuji assistant MCP sample tools (echo_ping, get_weather)"
        capabilities:
          tool: true

logging:
  level:
    root: INFO
    com.wuji.assistant: INFO
